<%# navigation styled for Bootstrap 3.0 %>
<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'Strona główna', root_path, class: 'navbar-brand' %>
      <% if current_user %>
            <div style="color:#f056f4" , class="navbar-brand">
              <%= current_user.email %> </div>
            <%= link_to "Dodaj post", 'entry/new', method: "create", class: 'navbar-brand' %>
            <%= link_to "Wyloguj", logout_path, method: "delete", class: 'navbar-brand', id: 'logout' %>
      <% else %>
            <%= link_to "Zaloguj", '/login', class: 'navbar-brand', id: 'login' %>
            <%= link_to "Rejestracja", '/signup', class: 'navbar-brand', id: 'register' %>
      <% end %>
    </div>
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
        <%= render 'layouts/navigation_links' %>
      </ul>
    </div>
  </div>
</nav>

